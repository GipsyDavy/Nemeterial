# Nombre de la aplicación
spring.application.name=nemeterial-backend

# Puerto del servidor
server.port=8080

# ????????????????????????????????????????????????
# BASE DE DATOS MySQL ? nombre real: nemeterial
# ????????????????????????????????????????????????
spring.datasource.url=jdbc:mysql://localhost:3306/nemeterial?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.username=root
spring.datasource.password=YeyeLaniNeme5**

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Línea corregida ? elige UNA de estas dos:
spring.jpa.hibernate.ddl-auto=validate
# spring.jpa.hibernate.ddl-auto=none     ? descomenta esta si validate da problemas

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# Seguridad básica desactivada (como la tenías originalmente)
spring.security.user.name= # deja vacío o comenta
spring.security.user.password= # deja vacío o comenta
management.security.enabled=false
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration